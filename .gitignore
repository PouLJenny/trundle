# 讨论记录 —— 含完整讨论内容,不要提交
# (.claude/trundle-discuss/ 是旧路径,本仓库的既有记录还在那里;.trundle/ 是新路径)
.claude/trundle-discuss/
.trundle/

# 本地测试用的项目级 skill 软链(指向本仓库的 skills/,不必提交)
.claude/skills/


.DS_Store
*.swp

# Python
__pycache__/
*.py[cod]

# 活体测试的运行产物(prompt 文件与原始 round plan)
tests/live/out/
